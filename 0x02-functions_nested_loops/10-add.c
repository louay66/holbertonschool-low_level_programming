#include <stdio.h>
#include "main.h"
/**
 * add- add a and b
 * @a : added
 * @b : added
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int add(int a, int b)
{
	return (a + b);
}

